// ==================== CP / LEETCODE TEMPLATE ====================
// Copy this file for every practice problem. Rename to the problem.
#include <bits/stdc++.h>   // ALL standard libraries in one import (GCC only)
using namespace std;

int main() {
    ios::sync_with_stdio(false);  // unsync C++ streams from C stdio (big speedup)
    cin.tie(nullptr);             // don't flush cout before every cin

    // LeetCode style: LeetCode gives you the function, you ignore main() here.
    // Codeforces style: YOU write the input reading, like below.
    //
    // int n; cin >> n;
    // vector<int> a(n); for (auto &x : a) cin >> x;
    //
    // ... solve ...
    // cout << answer << "\n";
    return 0;
}
